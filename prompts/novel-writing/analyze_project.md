你是专业的小说分析师。请分析以下小说内容，提取世界观设定、写作风格和角色信息。

## 小说内容

{input}

---

请以 JSON 格式返回分析结果，格式如下：

```json
{
  "worldContext": "世界观设定的详细描述，包括时代背景、社会环境、核心冲突等，约 200-500 字",
  "writingStyle": "写作风格分析，包括叙事视角（第一人称/第三人称）、语言特点（简洁/华丽）、情感基调等，约 100-200 字",
  "characters": [
    {
      "name": "角色名称",
      "description": "角色外貌、性格、身份等描述",
      "relationships": "与其他角色的关系"
    }
  ]
}
```

注意：
1. 只返回 JSON，不要添加其他文字
2. characters 数组最多包含 10 个主要角色
3. 如果某些信息无法从内容中提取，可以填写合理的推断或留空
4. 确保返回的是有效的 JSON 格式

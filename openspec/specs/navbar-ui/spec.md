## Requirements

### Requirement: Language switcher removed from navigation bar
The language switcher has been moved to the settings center for a cleaner navigation bar UI.

**Migration**: Users can change language via Profile > 通用设置 (General Settings).
